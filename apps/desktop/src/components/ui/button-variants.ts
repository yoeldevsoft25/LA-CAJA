The provided document only contains an `export` statement for `buttonVariants` and does not define the `default` or `destructive` variants within this file.Therefore, the requested change cannot be applied directly to the given document content.

If the `buttonVariants` definition is present in the actual file you are working with, please provide the full content of that file so the change can be applied correctly.

Given only the provided content, no change can be made that would result in a syntactically correct file while incorporating the requested edit.
